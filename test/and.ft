def (x: []int, val + tar: int) load -> int
    for val > 0
        val -= 1

        if x[val] == tar
            ret val
        end
    end
    ret -1
end

[43, 12, 53, 65, 22, 42] and -> arr
    def x: int = load(arr, 6, 65)
end

puts(arr, x)