mod os

/*
 * desc: 提示
 * deep: 睡觉
 * op:   结束
 */
def (desc: str, deep + op: int) loop
    do
        let i: int = 0
    for ->
        if i == op
            out ->
        end
        
        putl(i, desc)

        i += 1
        sleep(deep)
    end
end