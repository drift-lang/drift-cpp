def Foo 
    def (int) *add -> int

    def () f -> str
        ret "X"
    end
end

def Bar <- Foo
    def () a -> str
        ret f()
    end

    def (x: int) add -> int
        ret x + x
    end
end

putl(new Bar.a(), new Bar.add(200))